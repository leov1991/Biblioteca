@model Library.Models.Branches.BranchIndexModel
@{
    ViewBag.Title = "Lista de Sucursales";
}

<h3>@ViewBag.Title</h3>
<div class="branchIndex">
    <table class="mdl-data-table mdl-js-data-table mdl-data-table--selectable mdl-shadow--2dp customTable" id="branchIndexTable">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Abierto</th>
                <th>Cant. Items</th>
                <th>Cant. Miembros</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var branch in Model.Branches)
            {
                <tr>
                    <td class="strongTd"><a asp-controller="Branch" asp-action="Detail" asp-route-id="@branch.Id">@branch.Name</a></td>
                    <td>@branch.IsOpen</td>
                    <td>@branch.NumberOfAssets</td>
                    <td>@branch.NumberOfPatrons</td>
                </tr>

            }
        </tbody>


    </table>
</div>

